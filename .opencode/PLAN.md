## Wave 1: Voze Problem Space Heading

### Steps
1. **Update Project Metadata:**
   - Open `src/data/projects-meta.js`.
   - Locate the project with slug `voze-mobile-app-redesign`.
   - Add a `sections` array to this project object.
   - Add the first section: `{ type: 'section', heading: 'Problem Space', body: '' }`.

### Acceptance Criteria
- [ ] `projects-meta.js` is updated for `voze-mobile-app-redesign`.
- [ ] The section is correctly typed as `'section'`.
- [ ] The heading is exactly `"Problem Space"`.
- [ ] The page renders without errors and the heading is visible below the hero.
